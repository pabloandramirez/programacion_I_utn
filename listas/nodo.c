#include <stdio.h>

struct Nodo
{
    int dato;
    struct Nodo* siguiente;
};


int main(){
    
    void insertarAlFinal(struct ){
        struct Nodo* nuevoNodo = (struct Nodo*)malloc();
        nuevoNodo->dato = valor;
    }

    return 0;
}